server:
  port: 8080

spring:
  application:
    name: card-service
  cloud:
    loadbalancer:
      ribbon:
        enabled: false

---
spring:
  config:
    activate:
      on-profile: local

server:
  port: 8083
